title: RxJS Examples

===

-inherit example.plim

-block scripts
  = parent.scripts()
  script src='https://cdnjs.cloudflare.com/ajax/libs/rxjs/4.1.0/rx.all.min.js'

-block nav
  ul
    -for f in (f for f in PATH.parent.iterdir() if f.suffix == '.coffee')
      li
        a(href='${f.name}') ${f.stem}
